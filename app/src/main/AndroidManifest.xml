<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.midterm.hblx_02">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.HBLX02">
        <activity
            android:name=".View.Activity.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Activity.ContestWelcome">

        </activity>
        <activity android:name=".View.Activity.ContestDetail">

        </activity>
        <activity android:name=".View.Activity.TypeOfQuestionActivity">

        </activity>
        <activity android:name=".View.Activity.LearningActivity"></activity>
    </application>

</manifest>